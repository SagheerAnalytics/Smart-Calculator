# Smart-Calculator

# Smart Calculator (Groq-powered)

A Streamlit app that provides a basic calculator plus AI explanations using Groq.

## Run locally

1. Create a Python virtual environment:
   ```bash
   python -m venv .venv
   source .venv/bin/activate    # macOS/Linux
   .venv\Scripts\activate       # Windows# smart-calculator-groq
# Smart Calculator (Groq-powered)

A Streamlit app that provides a basic calculator plus AI explanations using Groq.

## Run locally

1. Create a Python virtual environment:
   ```bash
   python -m venv .venv
   source .venv/bin/activate    # macOS/Linux
   .venv\Scripts\activate       # Windows
